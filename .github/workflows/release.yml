name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout NextUI (build dependencies)
        uses: actions/checkout@v4
        with:
          repository: LoveRetro/NextUI
          path: NextUI

      - name: Checkout Music Player
        uses: actions/checkout@v4
        with:
          path: NextUI/workspace/nextui-music-player

      - name: Build for tg5040
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/NextUI/workspace:/root/workspace \
            ghcr.io/loveretro/tg5040-toolchain:latest \
            /bin/bash -c 'source ~/.bashrc && cd /root/workspace/tg5040/libmsettings && make build CROSS_COMPILE=aarch64-nextui-linux-gnu- PREFIX=/opt/nextui PREFIX_LOCAL=/opt/nextui && cd /root/workspace/nextui-music-player/src && make PLATFORM=tg5040'

      - name: Build for tg5050
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/NextUI/workspace:/root/workspace \
            ghcr.io/loveretro/tg5050-toolchain:latest \
            /bin/bash -c 'source ~/.bashrc && cp /root/workspace/nextui-music-player/bin/tg5050/libfdk-aac.so* $PREFIX/lib/ && cd /root/workspace/tg5050/libmsettings && make build CROSS_COMPILE=aarch64-nextui-linux-gnu- PREFIX=/opt/nextui PREFIX_LOCAL=/opt/nextui && cd /root/workspace/nextui-music-player/src && make PLATFORM=tg5050'

      - name: Package release zip
        run: |
          cd ${{ github.workspace }}/NextUI/workspace/nextui-music-player
          sudo rm -rf .github src .git .gitignore LICENSE README.md
          zip -r ../Music.Player.pak.zip .

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          gh release create "$VERSION" \
            --repo ${{ github.repository }} \
            --title "$VERSION" \
            --generate-notes \
            "${{ github.workspace }}/NextUI/workspace/Music.Player.pak.zip"
