#ifndef __DEFINES_H__
#define __DEFINES_H__
// Host-test stub. The real defines.h pulls in SDL via api.h; the units under
// test (watchdog.c) reference nothing from it, so an empty stub suffices.
#endif
